---
import { APP_BLOG } from 'astrowind:config';
import type { Post } from '~/types';

import Image from '~/components/common/Image.astro';

import { findImage } from '~/utils/images';
import { getPermalink } from '~/utils/permalinks';

export interface Props {
  post: Post;
}

const { post } = Astro.props;
const image = await findImage(post.image);

const link = APP_BLOG?.post?.isEnabled ? getPermalink(post.permalink, 'post') : '';
---

<article
  class="mb-6 transition intersect-once intersect-quarter motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade"
>
  <div class="relative w-full h-48 sm:h-56 md:h-64 overflow-hidden bg-gray-400 dark:bg-slate-700 rounded shadow-lg mb-6">
    {
      image &&
        (link ? (
          <a class="block w-full h-full" href={link}>
            <Image
              src={image}
              class="w-full h-full object-cover"
              widths={[400, 600]}
              width={600}
              sizes="(min-width: 1024px) 300px, (min-width: 768px) 350px, 400px"
              alt={post.title}
              layout="cover"
              loading="lazy"
              decoding="async"
            />
          </a>
        ) : (
          <Image
            src={image}
            class="w-full h-full object-cover"
            widths={[400, 600]}
            width={600}
            sizes="(min-width: 1024px) 300px, (min-width: 768px) 350px, 400px"
            alt={post.title}
            layout="cover"
            loading="lazy"
            decoding="async"
          />
        ))
    }
  </div>

  <h3 class="text-xl sm:text-2xl font-bold leading-tight mb-2 font-heading text-heading">
    {
      link ? (
        <a class="inline-block hover:text-primary transition ease-in duration-200" href={link}>
          {post.title}
        </a>
      ) : (
        post.title
      )
    }
  </h3>

  <p class="text-muted text-lg">{post.excerpt}</p>
</article>
